/*
package pro.notes.model.utils.types

const val PASSWORD_IDENTIFIER = "username"
val PASSWORD_FIELD_REGEX = BASIC_TEXT_FIELD_REGEX

class Password(
    password: String,
) : BasicTextField(password) {
    init {
        verifyField(PASSWORD_IDENTIFIER, PASSWORD_FIELD_REGEX)
    }
}
fun String.toPassword() = Password(this)
*/

/*
package pro.notes.model.utils.types

const val USERNAME_IDENTIFIER = "username"
val USERNAME_FIELD_REGEX = BASIC_TEXT_FIELD_REGEX

class Username(
    username: String,
) : BasicTextField(username) {
    init {
        verifyField(USERNAME_IDENTIFIER, USERNAME_FIELD_REGEX)
    }
}

fun String.toUsername() = Username(this)
*/
